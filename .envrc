# direnv allow .
PATH_add pj/bin
make -f pj/bin/Makefile -e TOP=$(dirname ${BASH_SOURCE}) gitignore

# && >&2 echo "ok $1" ||
have() { has $1 || >&2 echo "missing $1" ; }
have node
have npm
have tsc
have webstorm
have bun
have deno


